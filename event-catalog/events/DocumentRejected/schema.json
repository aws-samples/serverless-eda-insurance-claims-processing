{
  "openapi": "3.0.0",
  "info": {
    "version": "1.0.0",
    "title": "Document.Rejected"
  },
  "paths": {},
  "components": {
    "schemas": {
      "AWSEvent": {
        "type": "object",
        "required": ["detail-type", "resources", "detail", "id", "source", "time", "region", "version", "account"],
        "x-amazon-events-detail-type": "Document.Rejected",
        "x-amazon-events-source": "document.service",
        "properties": {
          "detail": {
            "$ref": "#/components/schemas/Document.Rejected"
          },
          "account": {
            "type": "string"
          },
          "detail-type": {
            "type": "string"
          },
          "id": {
            "type": "string"
          },
          "region": {
            "type": "string"
          },
          "resources": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "source": {
            "type": "string"
          },
          "time": {
            "type": "string",
            "format": "date-time"
          },
          "version": {
            "type": "string"
          }
        }
      },
      "Document.Rejected": {
        "type": "object",
        "required": ["customerId", "documentType"],
        "properties": {
          "customerId": {
            "type": "string"
          },
          "documentType": {
            "type": "string",
            "description": "The type of document that was rejected e.g. CAR, DRIVERS_LICENSE"
          },
          "reason": {
            "type": "string",
            "description": "The reason the document could not be processed"
          }
        }
      }
    }
  }
}
